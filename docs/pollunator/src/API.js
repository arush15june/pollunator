const URL = ''
const STATIONS_URI = '/api/stations'
const PUBLIC_KEY_URI = `/publickey`

const STATIONS_URL = `${URL}${STATIONS_URI}`
const PUBLIC_KEY_URL = `${URL}${PUBLIC_KEY_URI}`
export default URL;
export { STATIONS_URL, PUBLIC_KEY_URL }